// traderzview · public entry — createChart, createChartWith, the six series
// definitions. Re-exports from `api` and `extras` only (architecture §3.1).
// M0 bootstrap stub; implemented from M9 onward.
export {};
