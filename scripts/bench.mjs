// `bench` CI job — no-op until the bench harness + frame-time gates land (M10).
// Present from M0 so the pipeline shape is fixed (roadmap §2). Exits 0 (skipped).
console.log('bench: no frame-time scenarios until M10 — skipped.');
