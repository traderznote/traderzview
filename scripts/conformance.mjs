// `conformance` CI job — no-op until the Canvas-2D backend + conformance suite
// land (M6, roadmap §13). Present from M0 so the pipeline shape is fixed. Exits 0.
console.log('conformance: no backend fixtures until M6 — skipped.');
